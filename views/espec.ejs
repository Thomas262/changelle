<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Perfil de Usuario</title>
</head>
<body>
    <h1>Bienvenido, <%= user.name %></h1>
    <p>Correo electrónico: <%= user.email %></p>

    <% if (user.rol === 0) { %>
        <p>Rol: Usuario Espectador</p>
    <% } else if (user.rol === 1) { %>
        <p>Rol: Administrador
            <a href="/user/admin">propiedades</a>
        </p>
    <% } %>
    
    <a href="/user/logout">Cerrar sesión</a>
    <a href="/">Ver películas</a>
</body>
</html>
